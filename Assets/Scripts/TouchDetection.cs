using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TouchDetection : MonoBehaviour
{
	//private ProductionItemDisplay productionItem;

	//void Update()
	//	{
	//		if (Input.touchCount == 1)
	//		{
	//			Touch touch = Input.GetTouch(0);

	//			if (touch.phase == TouchPhase.Began)
	//			{
	//				Vector3 raycast = Camera.main.ScreenToWorldPoint(touch.position);
	//				RaycastHit2D hit = Physics2D.Raycast(raycast, Vector2.zero);

	//			hit.collider.gameObject.GetComponent<ProductionItemDisplay>().timer += 0.5F;

	//			if (hit.collider != null)
	//			{
	//				hit.collider.gameObject.GetComponent<ProductionItemDisplay>().timer += 0.5F;

	//			}
	//			//timeTouchEnded = Time.time;
	//			//	Debug.Log("Touched");
	//			//timer += 0.5F;
	//		}
	//		}
	//	}
}
